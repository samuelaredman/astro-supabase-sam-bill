---
export const prerender = false;
import { getSupabase, createSupabaseServerClientFromContext } from '../../utils/database'
import { igdbFetch } from '../../utils/igdb'

const supabase = getSupabase();
const { data: { user: _platformUser } } = await createSupabaseServerClientFromContext(Astro).auth.getUser();
const isLoggedIn = !!_platformUser;
const { slug } = Astro.params;

const { data: platform } = await supabase
  .from('platforms')
  .select('id, name, slug, igdb_id, banner_url, logo_url')
  .eq('slug', slug)
  .single()

if (!platform) return Astro.redirect('/404')

const { count: totalGameCount } = await supabase
  .from('game_platforms')
  .select('game_id', { count: 'exact', head: true })
  .eq('platform_id', platform.id)

const { data: gamePlatforms } = await supabase
  .from('game_platforms')
  .select(`games(id, title, slug, cover_img_url, date_released)`)
  .eq('platform_id', platform.id)
  .limit(3000)

const allGames = (gamePlatforms ?? [])
  .map((gp: any) => gp.games)
  .filter(Boolean)
  .sort((a: any, b: any) =>
    new Date(b.date_released ?? 0).getTime() - new Date(a.date_released ?? 0).getTime()
  )

const gameIds = allGames.map((g: any) => g.id)

const { data: gameReviewScores } = await supabase
  .from('reviews')
  .select('game_id, score')
  .in('game_id', gameIds.length > 0 ? gameIds : ['00000000-0000-0000-0000-000000000000'])
  .eq('status', 'published')

const reviewsByGame: Record<string, number[]> = {}
for (const r of gameReviewScores ?? []) {
  if (!reviewsByGame[r.game_id]) reviewsByGame[r.game_id] = []
  reviewsByGame[r.game_id].push(r.score)
}

function gameAvgScore(gameId: string): string | null {
  const scores = reviewsByGame[gameId]
  if (!scores || scores.length === 0) return null
  return (scores.reduce((a, b) => a + b, 0) / scores.length).toFixed(1)
}

function gameReviewCount(gameId: string): number {
  return reviewsByGame[gameId]?.length ?? 0
}

const { data: platformReviews } = await supabase
  .from('platform_reviews')
  .select(`id, score, title, body, created_at, profiles(id, username, avatar_url)`)
  .eq('platform_id', platform.id)
  .order('created_at', { ascending: false })

const platformReviewList = platformReviews ?? []
const platformScores = platformReviewList.map((r: any) => r.score)
const consoleAvg = platformScores.length > 0
  ? (platformScores.reduce((a: number, b: number) => a + b, 0) / platformScores.length).toFixed(1)
  : null
const consoleTotalRatings = platformScores.length

const allGameScores = Object.values(reviewsByGame).flat()
const gameAvg = allGameScores.length > 0
  ? (allGameScores.reduce((a, b) => a + b, 0) / allGameScores.length).toFixed(1)
  : null
const totalGameReviews = allGameScores.length

const topRated = allGames
  .sort((a: any, b: any) =>
    parseFloat(gameAvgScore(b.id) ?? '-1') - parseFloat(gameAvgScore(a.id) ?? '-1')
  )
  .slice(0, 100)

const byYear: Record<number, any[]> = {}
for (const g of allGames) {
  const year = g.date_released ? new Date(g.date_released).getFullYear() : 0
  if (!byYear[year]) byYear[year] = []
  byYear[year].push(g)
}

const byYearSorted: Record<number, any[]> = {}
for (const [year, games] of Object.entries(byYear)) {
  byYearSorted[parseInt(year)] = (games as any[])
    .sort((a: any, b: any) =>
      parseFloat(gameAvgScore(b.id) ?? '0') - parseFloat(gameAvgScore(a.id) ?? '0')
    )
    .slice(0, 25)
}

const years = Object.keys(byYearSorted)
  .map(Number)
  .filter(y => y > 0)
  .sort((a, b) => b - a)

let igdbData: any = null
if (platform.igdb_id) {
  const result = await igdbFetch('platforms',
    `fields name, summary, generation, platform_logo.url,
            platform_family.name,
            platform_versions.platform_version_release_dates.date,
            websites.url, websites.category;
     where id = ${platform.igdb_id};`)
  igdbData = result?.[0] ?? null
}

const platformDescriptions: Record<string, string> = {
  // Nintendo
  'switch':      'The Nintendo Switch is a hybrid video game console developed by Nintendo, released worldwide in March 2017. It can be used as a home console when docked, or as a handheld device when undocked. With over 140 million units sold it is one of the best-selling consoles of all time, home to landmark titles including Breath of the Wild, Super Mario Odyssey, and Animal Crossing: New Horizons.',
  'switch-2':    'The Nintendo Switch 2 is the successor to the original Nintendo Switch, developed by Nintendo. It features improved hardware, a larger screen, and full backward compatibility with Nintendo Switch games.',
  'nes':         'The Nintendo Entertainment System is an 8-bit home video game console released by Nintendo in North America in 1985. It revitalized the video game industry after the crash of 1983 and became the best-selling console of its era, introducing iconic franchises like Super Mario Bros., The Legend of Zelda, and Metroid.',
  'snes':        'The Super Nintendo Entertainment System is a 16-bit home video game console released by Nintendo in 1990. It competed with the Sega Genesis and is widely regarded as one of the greatest consoles ever made, featuring classics like Super Mario World, The Legend of Zelda: A Link to the Past, and Chrono Trigger.',
  'n64':         'The Nintendo 64 is a home video game console released by Nintendo in 1996. It was the last major home console to use ROM cartridges and introduced groundbreaking 3D games like Super Mario 64, The Legend of Zelda: Ocarina of Time, and GoldenEye 007.',
  'ngc':         'The Nintendo GameCube is a home video game console released by Nintendo in 2001. It was Nintendo\'s first console to use optical discs and featured beloved titles like The Legend of Zelda: The Wind Waker, Super Smash Bros. Melee, and Metroid Prime.',
  'wii':         'The Wii is a home video game console released by Nintendo in 2006. It introduced motion-control gaming with the Wii Remote and became a massive mainstream success, selling over 101 million units worldwide.',
  'wiiu':        'The Wii U is a home video game console released by Nintendo in 2012. It introduced the GamePad controller with a built-in touchscreen and featured titles like Super Mario 3D World and The Legend of Zelda: Breath of the Wild before being succeeded by the Nintendo Switch.',
  'gb':          'The Game Boy is a handheld game console released by Nintendo in 1989. It is one of the best-selling handheld consoles of all time, featuring iconic games like Tetris and Pokémon Red and Blue.',
  'gbc':         'The Game Boy Color is a handheld game console released by Nintendo in 1998. It added a color screen to the Game Boy line and was backward compatible with original Game Boy games.',
  'gba':         'The Game Boy Advance is a handheld game console released by Nintendo in 2001. It featured a 32-bit ARM processor and hosted an excellent library including Pokémon Ruby and Sapphire, Castlevania: Aria of Sorrow, and many SNES-quality ports.',
  '3ds':         'The Nintendo 3DS is a handheld game console released by Nintendo in 2011. It displays stereoscopic 3D effects without the need for glasses and sold over 75 million units, hosting a rich library including Pokémon X and Y, Fire Emblem Awakening, and The Legend of Zelda: A Link Between Worlds.',
  'new-3ds':     'The New Nintendo 3DS is an upgraded version of the Nintendo 3DS released in 2014. It features improved processing power, a second analog stick, and exclusive games and features over the original 3DS.',
  'famicom':     'The Family Computer, known as the Famicom in Japan, is the original Nintendo home video game console released in Japan in 1983. It was released internationally as the Nintendo Entertainment System and revitalized the global video game industry.',
  'fds':         'The Family Computer Disk System is a peripheral for the Famicom released by Nintendo in Japan in 1986. It uses proprietary floppy disks and was used to distribute many classic Nintendo titles in Japan.',
  'nds':         'The Nintendo DS is a dual-screen handheld game console released by Nintendo in 2004. It features a touchscreen on the lower display and sold over 154 million units, making it the best-selling handheld of all time.',
  'nintendo-dsi': 'The Nintendo DSi is an enhanced version of the Nintendo DS released in 2008. It added two cameras, an online shop, and a slightly larger screen while removing the Game Boy Advance slot.',
  'virtualboy':  'The Virtual Boy is a tabletop portable game console released by Nintendo in 1995. It used a monochrome red LED display to create a stereoscopic 3D effect and was a commercial failure, discontinued after less than a year on the market.',
  'sfam':        'The Super Famicom is the Japanese version of the Super Nintendo Entertainment System, released by Nintendo in 1990. It is the Japanese counterpart to the SNES and shares the same library of iconic 16-bit games.',
  'satellaview': 'The Satellaview is a satellite modem peripheral for the Super Famicom released by Nintendo in Japan in 1995. It allowed players to download games and magazines via satellite broadcast.',
  'g-and-w':     'Game & Watch is a line of handheld electronic games produced by Nintendo from 1980 to 1991. They were the first handheld gaming devices made by Nintendo and introduced the cross-shaped D-pad that became standard in game controllers.',
  // PlayStation
  'ps':          'The PlayStation is a home video game console released by Sony in 1994. It was Sony\'s first video game console and sold over 102 million units, featuring landmark titles like Final Fantasy VII, Metal Gear Solid, and Resident Evil.',
  'ps2':         'The PlayStation 2 is a home video game console released by Sony in 2000. It is the best-selling video game console of all time with over 155 million units sold. Its massive library includes Grand Theft Auto: San Andreas, Shadow of the Colossus, Kingdom Hearts, and God of War.',
  'ps3':         'The PlayStation 3 is a home video game console released by Sony in 2006. It introduced Blu-ray as a standard format and featured iconic exclusives like Uncharted 2, The Last of Us, Metal Gear Solid 4, and LittleBigPlanet.',
  'ps4--1':      'The PlayStation 4 is a home video game console released by Sony in 2013. It sold over 117 million units and is home to acclaimed exclusives like The Last of Us Part II, God of War, Horizon Zero Dawn, Bloodborne, and Spider-Man.',
  'ps5':         'The PlayStation 5 is a home video game console released by Sony in 2020. It features ultra-fast SSD storage, ray tracing, 4K resolution at up to 120fps, and the DualSense controller with haptic feedback and adaptive triggers.',
  'psp':         'The PlayStation Portable is a handheld game console released by Sony in 2005. It was Sony\'s first handheld console and featured a large widescreen display, UMD media format, and titles like Crisis Core: Final Fantasy VII and God of War: Chains of Olympus.',
  'psvita':      'The PlayStation Vita is a handheld game console released by Sony in 2011. It featured an OLED touchscreen, dual analog sticks, and a large library of indie and Japanese titles. Despite a passionate fanbase it was discontinued in 2019.',
  'psvr':        'PlayStation VR is a virtual reality headset released by Sony in 2016 for use with the PlayStation 4. It brought affordable VR gaming to console players and featured titles like Astro Bot Rescue Mission and Resident Evil 7.',
  'psvr2':       'PlayStation VR2 is Sony\'s second virtual reality headset released in 2023 for the PlayStation 5. It features eye tracking, adaptive triggers, and inside-out tracking for an improved VR experience.',
  // Xbox
  'xbox':        'The Xbox is a home video game console released by Microsoft in 2001. It was Microsoft\'s first video game console and introduced the original Xbox Live online service, featuring titles like Halo: Combat Evolved and Fable.',
  'xbox360':     'The Xbox 360 is a home video game console released by Microsoft in 2005. It sold over 84 million units and pioneered online console gaming with Xbox Live, featuring titles like Halo 3, Gears of War, and Mass Effect.',
  'xboxone':     'The Xbox One is a home video game console released by Microsoft in 2013. It introduced Game Pass and backward compatibility features, selling over 50 million units during its lifetime.',
  'series-x-s':  'The Xbox Series X|S are Microsoft\'s ninth-generation consoles released in November 2020. The Series X is the flagship 4K model while the Series S is a compact digital-only console. Both feature near-instant load times, full backward compatibility, and Xbox Game Pass integration.',
  // Sega
  'genesis-slash-megadrive': 'The Sega Mega Drive, known as the Genesis in North America, is a 16-bit home video game console released by Sega in 1988. It was the primary competitor to the Super Nintendo and featured iconic titles like Sonic the Hedgehog, Streets of Rage, and Phantasy Star IV.',
  'dc':          'The Dreamcast is a home video game console released by Sega in 1998. It was Sega\'s last console and the first to include a built-in modem for online gaming. Despite being ahead of its time with titles like Shenmue and Jet Set Radio, it was discontinued in 2001.',
  'saturn':      'The Sega Saturn is a home video game console released by Sega in 1994. It competed with the original PlayStation and featured excellent 2D games as well as titles like Nights into Dreams and Panzer Dragoon Saga.',
  'sms':         'The Sega Master System is an 8-bit home video game console released by Sega in 1985. It competed with the Nintendo Entertainment System and was more successful in Europe and Brazil than in North America.',
  'sega32':      'The Sega 32X is an add-on for the Sega Mega Drive released in 1994. It was designed to extend the life of the Genesis with 32-bit capabilities but was ultimately a commercial failure due to its short lifespan.',
  'sega-cd':     'The Sega CD is a CD-ROM add-on for the Sega Genesis released in 1991. It added CD audio, full-motion video, and a faster processor to the Genesis library.',
  'gamegear':    'The Sega Game Gear is a handheld game console released by Sega in 1990. It was the primary competitor to the Game Boy and featured a backlit color screen, though its battery life was significantly shorter.',
  // PC & Mobile
  'win':         'PC gaming on Microsoft Windows represents the most versatile gaming platform, offering access to the largest game library through storefronts like Steam, Epic Games Store, and GOG. PCs can be upgraded over time and support everything from indie games to the most graphically demanding titles.',
  'mac':         'Mac gaming refers to playing video games on Apple\'s macOS platform. While historically limited compared to Windows PC gaming, the Mac library has grown significantly with Apple Silicon chips providing strong gaming performance and support for ports of major titles.',
  'linux':       'Linux gaming has grown substantially thanks to Valve\'s Proton compatibility layer and SteamOS, which allows many Windows games to run on Linux. It is the platform powering the Steam Deck.',
  'dos':         'DOS gaming refers to games released for the MS-DOS operating system, primarily during the 1980s and 1990s. This era produced landmark titles across every genre including Doom, Quake, Commander Keen, and countless strategy games.',
  'android':     'Android is Google\'s mobile operating system powering the majority of the world\'s smartphones. It hosts a vast library of games through the Google Play Store, from casual mobile titles to increasingly complex premium experiences.',
  'ios':         'iOS is Apple\'s mobile operating system for iPhone and iPad. It hosts a large library of games through the App Store, from casual titles to premium experiences and high-quality console ports.',
  // Atari
  'atari2600':   'The Atari 2600 is a home video game console released by Atari in 1977. It popularized the use of ROM cartridges and was the dominant home console of the late 1970s and early 1980s, featuring classics like Space Invaders and Pitfall.',
  'atari5200':   'The Atari 5200 is a home video game console released by Atari in 1982. It was intended as a more powerful successor to the 2600 but suffered from controller reliability issues and was discontinued in 1984.',
  'atari7800':   'The Atari 7800 is a home video game console released by Atari in 1986. It was backward compatible with Atari 2600 games and competed with the NES, though it struggled to match Nintendo\'s first-party lineup.',
  'jaguar':      'The Atari Jaguar is a home video game console released by Atari in 1993. It was marketed as the first 64-bit gaming system, though its library was limited and it was discontinued in 1996.',
  'lynx':        'The Atari Lynx is a handheld game console released by Atari in 1989. It was the first handheld with a color backlit LCD screen and featured impressive arcade ports, but its battery consumption limited its appeal against the Game Boy.',
  // Other notable platforms
  'arcade':      'Arcade gaming refers to coin-operated entertainment machines typically found in public venues. The golden age of arcades in the late 1970s and 1980s produced iconic games like Pac-Man, Donkey Kong, Street Fighter II, and Mortal Kombat that defined entire genres.',
  'colecovision': 'The ColecoVision is a home video game console released by Coleco in 1982. It offered near-arcade-quality graphics for its time and featured an expansion port that allowed it to play Atari 2600 games.',
  'intellivision': 'The Intellivision is a home video game console released by Mattel in 1979. It was the primary competitor to the Atari 2600 and featured a unique controller with a numeric keypad.',
  'amiga':       'The Amiga is a family of personal computers released by Commodore from 1985. They were known for their advanced graphics and sound capabilities and hosted a thriving gaming scene through the late 1980s and early 1990s.',
  'c64':         'The Commodore 64 is a home computer released by Commodore in 1982. It is the best-selling single computer model of all time and hosted an enormous library of games, making it one of the most important gaming platforms of the 1980s.',
  'zxs':         'The ZX Spectrum is a home computer released by Sinclair Research in 1982. It was hugely popular in the United Kingdom and hosted a large library of games, making it one of the most influential computers in European gaming history.',
  'turbografx16--1': 'The TurboGrafx-16, known as the PC Engine in Japan, is a home video game console released by NEC in 1987. It competed with the NES and Mega Drive and featured an excellent library of shooters and action games.',
  'neogeoaes':   'The Neo Geo AES is a home video game console released by SNK in 1990. It used the same hardware as SNK\'s arcade machines, providing true arcade-quality games at home, though at a very high price point.',
  'ouya':        'The Ouya is an Android-based microconsole released in 2013. It was funded through Kickstarter and focused on free-to-play and indie games, but struggled to gain traction and was discontinued in 2015.',
  'stadia':      'Google Stadia was a cloud gaming service launched by Google in 2019. It allowed players to stream games directly to their browser or Chromecast without dedicated hardware, but was shut down by Google in January 2023.',
}

const platformReleaseDates: Record<string, string> = {
  'switch': 'March 3, 2017',
  'switch-2': '2025',
  'nes': 'October 18, 1985',
  'snes': 'August 23, 1991',
  'n64': 'September 29, 1996',
  'ngc': 'November 18, 2001',
  'wii': 'November 19, 2006',
  'wiiu': 'November 18, 2012',
  'gb': 'April 21, 1989',
  'gbc': 'October 21, 1998',
  'gba': 'March 21, 2001',
  '3ds': 'February 26, 2011',
  'new-3ds': 'October 11, 2014',
  'nds': 'November 21, 2004',
  'nintendo-dsi': 'November 1, 2008',
  'famicom': 'July 15, 1983',
  'sfam': 'November 21, 1990',
  'virtualboy': 'July 21, 1995',
  'ps': 'December 3, 1994',
  'ps2': 'March 4, 2000',
  'ps3': 'November 11, 2006',
  'ps4--1': 'November 15, 2013',
  'ps5': 'November 12, 2020',
  'psp': 'December 12, 2004',
  'psvita': 'December 17, 2011',
  'xbox': 'November 15, 2001',
  'xbox360': 'November 22, 2005',
  'xboxone': 'November 22, 2013',
  'series-x-s': 'November 10, 2020',
  'genesis-slash-megadrive': 'October 29, 1988',
  'dc': 'November 27, 1998',
  'saturn': 'November 22, 1994',
  'sms': 'October 20, 1986',
  'gamegear': 'October 6, 1990',
  'atari2600': 'September 11, 1977',
  'atari7800': 'May 1986',
  'jaguar': 'November 23, 1993',
  'lynx': 'September 1, 1989',
  'arcade': 'N/A',
  'win': 'N/A',
  'mac': 'N/A',
  'linux': 'N/A',
  'dos': 'N/A',
  'android': 'N/A',
  'ios': 'N/A',
}

const platformGenerations: Record<string, string> = {
  'switch': '8th',
  'switch-2': '9th',
  'nes': '3rd',
  'snes': '4th',
  'sfam': '4th',
  'n64': '5th',
  'ngc': '6th',
  'wii': '7th',
  'wiiu': '8th',
  'gb': '4th',
  'gbc': '5th',
  'gba': '6th',
  '3ds': '8th',
  'new-3ds': '8th',
  'nds': '7th',
  'ps': '5th',
  'ps2': '6th',
  'ps3': '7th',
  'ps4--1': '8th',
  'ps5': '9th',
  'psp': '7th',
  'psvita': '8th',
  'xbox': '6th',
  'xbox360': '7th',
  'xboxone': '8th',
  'series-x-s': '9th',
  'genesis-slash-megadrive': '4th',
  'dc': '6th',
  'saturn': '5th',
  'sms': '3rd',
  'gamegear': '4th',
  'atari2600': '2nd',
  'atari7800': '3rd',
  'jaguar': '5th',
  'lynx': '4th',
}

const platformFamilies: Record<string, string> = {
  'switch': 'Nintendo',
  'switch-2': 'Nintendo',
  'nes': 'Nintendo',
  'snes': 'Nintendo',
  'sfam': 'Nintendo',
  'famicom': 'Nintendo',
  'fds': 'Nintendo',
  'n64': 'Nintendo',
  'ngc': 'Nintendo',
  'wii': 'Nintendo',
  'wiiu': 'Nintendo',
  'gb': 'Nintendo',
  'gbc': 'Nintendo',
  'gba': 'Nintendo',
  '3ds': 'Nintendo',
  'new-3ds': 'Nintendo',
  'nds': 'Nintendo',
  'nintendo-dsi': 'Nintendo',
  'virtualboy': 'Nintendo',
  'satellaview': 'Nintendo',
  'g-and-w': 'Nintendo',
  'ps': 'PlayStation',
  'ps2': 'PlayStation',
  'ps3': 'PlayStation',
  'ps4--1': 'PlayStation',
  'ps5': 'PlayStation',
  'psp': 'PlayStation',
  'psvita': 'PlayStation',
  'psvr': 'PlayStation',
  'psvr2': 'PlayStation',
  'xbox': 'Xbox',
  'xbox360': 'Xbox',
  'xboxone': 'Xbox',
  'series-x-s': 'Xbox',
  'genesis-slash-megadrive': 'Sega',
  'dc': 'Sega',
  'saturn': 'Sega',
  'sms': 'Sega',
  'sega32': 'Sega',
  'sega-cd': 'Sega',
  'gamegear': 'Sega',
  'sg1000': 'Sega',
  'atari2600': 'Atari',
  'atari5200': 'Atari',
  'atari7800': 'Atari',
  'atari8bit': 'Atari',
  'jaguar': 'Atari',
  'atari-jaguar-cd': 'Atari',
  'lynx': 'Atari',
  'atari-st': 'Atari',
  'win': 'PC',
  'mac': 'Apple',
  'linux': 'PC',
  'dos': 'PC',
  'android': 'Mobile',
  'ios': 'Apple',
  'arcade': 'Arcade',
  'colecovision': 'Coleco',
  'intellivision': 'Mattel',
  'neogeoaes': 'SNK',
  'neogeomvs': 'SNK',
  'neo-geo-cd': 'SNK',
  'neo-geo-pocket-color': 'SNK',
  'turbografx16--1': 'NEC',
  'turbografx-16-slash-pc-engine-cd': 'NEC',
  'amiga': 'Commodore',
  'amiga-cd32': 'Commodore',
  'c64': 'Commodore',
  'c16': 'Commodore',
  'vic-20': 'Commodore',
  'zxs': 'Sinclair',
  'acpc': 'Amstrad',
  'stadia': 'Google',
  'ouya': 'Ouya',
  '3do': '3DO',
}

const consoleBanners: Record<string, string> = {
  'switch':                  '/banners/nintendo-switch.jpg',
  'switch-2':                '/banners/nintendo-switch-2.jpg',
  'ps5':                     '/banners/playstation-5.jpg',
  'ps4--1':                  '/banners/playstation-4.jpg',
  'ps3':                     '/banners/playstation-3.jpg',
  'ps2':                     '/banners/playstation-2.jpg',
  'ps':                      '/banners/playstation-1.jpg',
  'series-x-s':              '/banners/xbox-series-x.jpg',
  'xboxone':                 '/banners/xbox-one.jpg',
  'xbox360':                 '/banners/xbox-360.jpg',
  'xbox':                    '/banners/xbox.jpg',
  'wii':                     '/banners/wii.jpg',
  'wiiu':                    '/banners/wii-u.jpg',
  'n64':                     '/banners/nintendo-64.jpg',
  'ngc':                     '/banners/gamecube.jpg',
  'snes':                    '/banners/super-nintendo.jpg',
  'nes':                     '/banners/nes.jpg',
  '3ds':                     '/banners/nintendo-3ds.jpg',
  'new-3ds':                 '/banners/nintendo-3ds.jpg',
  'nds':                     '/banners/nintendo-ds.jpg',
  'gba':                     '/banners/game-boy-advance.jpg',
  'dc':                      '/banners/dreamcast.jpg',
  'genesis-slash-megadrive': '/banners/sega-genesis.jpg',
  'saturn':                  '/banners/sega-saturn.jpg',
  'win':                     '/banners/pc.jpg',
}
const bannerImage = (platform as any).banner_url || consoleBanners[platform.slug] || null

const summary = igdbData?.summary || platformDescriptions[platform.slug] || null
const releaseDate = platformReleaseDates[platform.slug] ?? null
const releaseYearDisplay = releaseDate ? releaseDate.split(' ').pop() ?? null : null
const generation = igdbData?.generation ? `${igdbData.generation}th` : platformGenerations[platform.slug] ?? null
const family = igdbData?.platform_family?.name ?? platformFamilies[platform.slug] ?? null
const officialSite = igdbData?.websites?.find((w: any) => w.category === 1)?.url ?? null

function igdbImage(url: string | null, size = 't_cover_big') {
  if (!url) return null
  const clean = url.startsWith('//') ? `https:${url}` : url
  return clean.replace(/t_[a-z_]+/, size)
}

const mosaicCovers = topRated
  .map((g: any) => igdbImage(g.cover_img_url, 't_cover_big'))
  .filter(Boolean) as string[]
const mosaicTiles: string[] = mosaicCovers.length > 0
  ? [...mosaicCovers, ...mosaicCovers, ...mosaicCovers].slice(0, 24)
  : []
const hasMosaic = mosaicTiles.length >= 6

function scoreClass(score: number): string {
  if (score === 10) return 'perfect'
  if (score >= 9)   return 'great'
  if (score >= 8)   return 'teal'
  if (score >= 7)   return 'good'
  if (score >= 5)   return 'mid'
  if (score >= 3)   return 'orange'
  return 'low'
}

function timeAgo(dateStr: string): string {
  const diff = Math.floor((Date.now() - new Date(dateStr).getTime()) / 1000)
  if (diff < 60) return `${diff}s`
  if (diff < 3600) return `${Math.floor(diff / 60)}m`
  if (diff < 86400) return `${Math.floor(diff / 3600)}h`
  if (diff < 604800) return `${Math.floor(diff / 86400)}d`
  return `${Math.floor(diff / 604800)}w`
}

function getInitials(username: string): string {
  return username.slice(0, 2).toUpperCase()
}

const consoleScoreClass = consoleAvg ? scoreClass(parseFloat(consoleAvg)) : null
const gameAvgScoreClass = gameAvg ? scoreClass(parseFloat(gameAvg)) : null
const displayGameCount = totalGameCount ?? allGames.length
const activeTab = Astro.url.searchParams.get('tab') || 'overview'

// per-year game counts (all games, not just top 25) + avg scores for histogram
const yearGameCountsMap: Record<number, number> = {}
const yearAvgScoresMap: Record<number, number | null> = {}
for (const year of years) {
  const gamesInYear = byYear[year] ?? []
  yearGameCountsMap[year] = gamesInYear.length
  const allScores: number[] = []
  for (const g of gamesInYear) {
    const scores = reviewsByGame[g.id]
    if (scores?.length > 0) allScores.push(...scores)
  }
  yearAvgScoresMap[year] = allScores.length > 0
    ? Math.round((allScores.reduce((a, b) => a + b, 0) / allScores.length) * 10) / 10
    : null
}
const yearGameCountsJson = JSON.stringify(yearGameCountsMap)
const yearAvgScoresJson = JSON.stringify(yearAvgScoresMap)

// platform family accent colors
const accentByFamily: Record<string, [string, string, string]> = {
  'Nintendo':    ['#c8332a', '#e84444', '232,68,68'],
  'PlayStation': ['#003087', '#2f6ec4', '47,110,196'],
  'Xbox':        ['#0e7a0d', '#52b043', '82,176,67'],
  'Sega':        ['#15499e', '#3a7fcd', '58,127,205'],
  'PC':          ['#0063b1', '#0096d4', '0,150,212'],
  'Apple':       ['#444444', '#909090', '144,144,144'],
  'Mobile':      ['#0063b1', '#0096d4', '0,150,212'],
  'Atari':       ['#c84a0a', '#e8611a', '232,97,26'],
  'Arcade':      ['#8b3a8b', '#c56fc5', '197,111,197'],
  'SNK':         ['#b84040', '#d46060', '212,96,96'],
  'NEC':         ['#6a1090', '#9a3fc0', '154,63,192'],
  'Commodore':   ['#c8832a', '#e8a034', '232,160,52'],
  'Sinclair':    ['#1a5c8a', '#3a8fba', '58,143,186'],
  'Google':      ['#4285f4', '#6a9ef7', '106,158,247'],
}
const [accentLight, accentDark, accentRgb] = accentByFamily[family ?? ''] ?? ['#c8332a', '#e84444', '232,68,68']

const reviewedGameCount = allGames.filter((g: any) => (reviewsByGame[g.id]?.length ?? 0) > 0).length

const gameAvgScoresList: number[] = allGames
  .filter((g: any) => reviewsByGame[g.id]?.length > 0)
  .map((g: any) => {
    const scores = reviewsByGame[g.id]
    return scores.reduce((a: number, b: number) => a + b, 0) / scores.length
  })

const gameScoreDist = Array.from({ length: 10 }, (_, i) => {
  if (i === 9) return gameAvgScoresList.filter(avg => avg >= 9.5).length
  return gameAvgScoresList.filter(avg => avg >= i + 0.5 && avg < i + 1.5).length
})

const reviewsByYearMap: Record<number, number> = {}
for (const g of allGames) {
  const year = g.date_released ? new Date(g.date_released).getFullYear() : 0
  if (!year) continue
  const rc = reviewsByGame[g.id]?.length ?? 0
  if (rc > 0) reviewsByYearMap[year] = (reviewsByYearMap[year] ?? 0) + rc
}
const reviewsByYearData = Object.entries(reviewsByYearMap)
  .map(([year, count]) => ({ year: parseInt(year), count }))
  .sort((a, b) => a.year - b.year)

const topReviewedGames = allGames
  .filter((g: any) => (reviewsByGame[g.id]?.length ?? 0) > 0)
  .sort((a: any, b: any) => (reviewsByGame[b.id]?.length ?? 0) - (reviewsByGame[a.id]?.length ?? 0))
  .slice(0, 5)
  .map((g: any) => {
    const avg = gameAvgScore(g.id)
    const count = gameReviewCount(g.id)
    const sc = avg ? scoreClass(parseFloat(avg)) : null
    return {
      title: g.title,
      slug: g.slug,
      cover: igdbImage(g.cover_img_url),
      reviews: count,
      avg,
      badge: sc ? `badge-${sc}` : null,
    }
  })

const platformStatsJson = JSON.stringify({
  gameCount: displayGameCount,
  totalGameReviews,
  reviewedGameCount,
  gameAvg: gameAvg ? parseFloat(gameAvg) : null,
  consoleAvg: consoleAvg ? parseFloat(consoleAvg) : null,
  consoleTotalRatings,
  gameScoreDist,
  reviewsByYearData,
  topReviewedGames,
})

function serializeGames(games: any[]) {
  return games.map((g: any) => {
    const avg = gameAvgScore(g.id)
    const count = gameReviewCount(g.id)
    const sc = avg ? scoreClass(parseFloat(avg)) : null
    return {
      title: g.title,
      slug: g.slug,
      cover: igdbImage(g.cover_img_url),
      score: avg,
      badge: sc ? `badge-${sc}` : null,
      reviews: count > 0 ? `${count} ${count === 1 ? 'review' : 'reviews'}` : null,
    }
  })
}

const topRatedJson = JSON.stringify(serializeGames(topRated))
const byYearJson = JSON.stringify(
  years.reduce((acc: any, year: number) => {
    acc[year] = serializeGames(byYearSorted[year] ?? [])
    return acc
  }, {})
)
const yearsJson = JSON.stringify(years)

// CDN cache — anonymous visitors share a cached response; auth users get a fresh render
if (!isLoggedIn) {
  Astro.response.headers.set('Netlify-CDN-Cache-Control', 'public, max-age=300, stale-while-revalidate=600');
  Astro.response.headers.set('Cache-Control', 'no-store');
} else {
  Astro.response.headers.set('Cache-Control', 'private, no-store');
}
Astro.response.headers.set('Netlify-Vary', 'cookie=sb-bzlwwtatoiyzwirerify-auth-token');
---

<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Runs synchronously before CSS is applied — prevents dark mode flash -->
  <script is:inline>
    (function() {
      var t = localStorage.getItem('theme') ||
        (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      document.documentElement.setAttribute('data-theme', t);
      var m = document.getElementById('meta-theme-color');
      if (m) m.content = t === 'dark' ? '#0f0f0e' : '#f9f8f6';
    })();
  </script>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" id="meta-theme-color" content="#f9f8f6" />
  <title>{platform.name} — Chekpoint</title>
  <meta name="description" content={summary?.slice(0, 160) ?? `Browse all ${platform.name} games and reviews on Chekpoint.`} />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&family=DM+Serif+Display&family=Press+Start+2P&display=swap" rel="stylesheet" />
  <script is:inline define:vars={{ accentLight, accentDark, accentRgb }}>
    (function() {
      var t = document.documentElement.getAttribute('data-theme') || 'dark';
      document.documentElement.style.setProperty('--game-accent', t === 'light' ? accentLight : accentDark);
      document.documentElement.style.setProperty('--accent-rgb', accentRgb);
    })();
  </script>
  <style is:global>
    :root[data-theme="light"] {
      --bg:#f5f4f2;--bg-card:#ffffff;--bg-hover:#f4f3f0;
      --border:#e8e6e1;--border-subtle:#f0ede8;
      --text-primary:#141412;--text-secondary:#6b6860;--text-tertiary:#a09d98;
      --tag-bg:#f0ede8;--tag-text:#6b6860;
      --shadow:0 1px 3px rgba(0,0,0,.06);--shadow-md:0 4px 16px rgba(0,0,0,.1);
      --score-great:#1a6b3c;--score-great-bg:#eaf5ee;
      --score-good:#1a4a6b;--score-good-bg:#eaf0f5;
      --score-mid:#6b5a1a;--score-mid-bg:#f5f0ea;
      --score-low:#6b1a1a;--score-low-bg:#f5eaea;
      --game-accent:#c8332a;
      --hero-overlay:linear-gradient(to bottom,rgba(245,244,242,0)0%,rgba(245,244,242,.6)65%,rgba(245,244,242,1)100%);
      --hero-side:linear-gradient(to right,rgba(245,244,242,.3)0%,transparent 50%);
    }
    :root[data-theme="dark"] {
      --bg:#09090a;--bg-card:#131314;--bg-hover:#1c1c1e;
      --border:#222224;--border-subtle:#191919;
      --text-primary:#f0ede8;--text-secondary:#908d87;--text-tertiary:#484644;
      --tag-bg:#1c1c1e;--tag-text:#6a6866;
      --shadow:0 1px 3px rgba(0,0,0,.5);--shadow-md:0 4px 16px rgba(0,0,0,.7);
      --score-great:#4ade80;--score-great-bg:#112e1c;
      --score-teal:#2dd4bf;--score-teal-bg:#082d2a;
      --score-good:#60a5fa;--score-good-bg:#0e2240;
      --score-mid:#fbbf24;--score-mid-bg:#271e00;
      --score-orange:#fb923c;--score-orange-bg:#2c160a;
      --score-low:#f87171;--score-low-bg:#300e0e;
      --score-perfect:#ffffff;--score-perfect-bg:#22223a;
      --game-accent:#e84444;
      --hero-overlay:linear-gradient(to bottom,rgba(9,9,10,0)0%,rgba(9,9,10,.65)65%,rgba(9,9,10,1)100%);
      --hero-side:linear-gradient(to right,rgba(9,9,10,.35)0%,transparent 50%);
    }

    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    html{scroll-behavior:smooth}
    body{font-family:'DM Sans',sans-serif;background:var(--bg);color:var(--text-primary);min-height:100vh;-webkit-font-smoothing:antialiased}
    a{color:inherit;text-decoration:none}

    .topnav{position:sticky;top:0;z-index:30;background:rgba(9,9,10,.88);border-bottom:1px solid var(--border);padding:13px 28px;display:flex;align-items:center;backdrop-filter:blur(20px)}
    [data-theme="light"] .topnav{background:rgba(245,244,242,.88)}
    .back-btn{display:flex;align-items:center;gap:6px;font-size:13px;color:var(--text-tertiary);transition:color .15s;background:none;border:none;cursor:pointer;font-family:inherit;padding:0}
    .back-btn:hover{color:var(--text-primary)}
    .logo{font-family:'Press Start 2P',monospace;font-size:13px;line-height:1;display:flex;align-items:center;gap:7px;margin-left:auto}
    .logo-dot{width:6px;height:6px;background:var(--game-accent);border-radius:50%;box-shadow:0 0 10px var(--game-accent)}
    .theme-btn{background:none;border:none;cursor:pointer;color:var(--text-tertiary);padding:6px;display:flex;align-items:center;transition:color .15s;margin-left:8px}
    .theme-btn:hover{color:var(--text-primary)}

    .jump-nav{position:sticky;top:53px;z-index:20;background:rgba(9,9,10,.72);border-bottom:1px solid var(--border);backdrop-filter:blur(16px);display:flex;overflow-x:auto;scrollbar-width:none}
    .jump-nav::-webkit-scrollbar{display:none}
    [data-theme="light"] .jump-nav{background:rgba(245,244,242,.82)}
    .jump-link{font-size:11px;font-weight:500;color:var(--text-tertiary);padding:10px 20px;letter-spacing:.08em;text-transform:uppercase;transition:color .15s,border-color .15s;border-bottom:2px solid transparent;white-space:nowrap;flex-shrink:0}
    .jump-link:hover{color:var(--text-primary)}
    .jump-link.active{color:var(--text-primary);border-color:var(--game-accent)}

    .hero{position:relative;width:100%;height:200px;overflow:hidden;background:#0c0c10}
    .hero-img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;object-position:center 40%;opacity:.78}
    .hero-mosaic{position:absolute;inset:-20px;display:none;flex-wrap:wrap;filter:blur(18px) saturate(1.5) brightness(.35);transform:scale(1.08)}
    .hero-mosaic.visible{display:flex}
    .hero-mosaic-tile{flex:1 1 90px;min-width:90px;min-height:180px;background-size:cover;background-position:center}
    .hero-side-fade{position:absolute;inset:0;background:var(--hero-side)}
    .hero-overlay{position:absolute;inset:0;background:var(--hero-overlay)}

    .page{max-width:900px;margin:0 auto;padding:0 28px 100px}

    .platform-header{display:flex;gap:28px;align-items:flex-end;margin-bottom:32px;position:relative;z-index:2}
    .platform-header.has-banner{margin-top:-52px}
    .platform-header.no-banner{margin-top:32px}

    .platform-logo-wrap{flex-shrink:0;width:140px;height:140px;border-radius:22px;background:var(--bg-card);border:1px solid rgba(var(--accent-rgb),.3);box-shadow:0 14px 48px rgba(0,0,0,.8),0 0 36px rgba(var(--accent-rgb),.18);display:flex;align-items:center;justify-content:center;overflow:hidden;font-size:60px}
    .platform-logo{width:100%;height:100%;object-fit:contain;padding:18px;filter:brightness(0) invert(1)}
    [data-theme="light"] .platform-logo{filter:brightness(0);opacity:.85}
    .platform-logo.custom{filter:none;opacity:1;padding:0;object-fit:cover}
    .platform-logo-wrap.custom{border:none;background:none;box-shadow:0 14px 48px rgba(0,0,0,.8)}
    .platform-info{flex:1;min-width:0;padding-bottom:6px}
    .platform-name-row{display:flex;align-items:baseline;gap:14px;margin-bottom:8px;flex-wrap:wrap}
    .platform-name{font-family:'Cinzel',serif;font-size:34px;line-height:1.1;letter-spacing:.04em;font-weight:600;text-shadow:0 2px 24px rgba(0,0,0,.8)}
    .console-score-wrap{display:flex;align-items:baseline;gap:5px;flex-shrink:0}
    .console-score-num{font-family:'Century Schoolbook','New Century Schoolbook',Georgia,serif;font-size:28px;font-weight:400;line-height:1;letter-spacing:-1px}
    .console-score-num.great{color:var(--score-great)}
    .console-score-num.good{color:var(--score-good)}
    .console-score-num.mid{color:var(--score-mid)}
    .console-score-num.low{color:var(--score-low)}
    .console-score-label{font-size:11px;color:var(--text-tertiary);font-weight:500;text-transform:uppercase;letter-spacing:.08em;white-space:nowrap}
    .platform-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .platform-meta-item{font-size:13px;color:var(--text-secondary)}
    .platform-meta-sep{color:var(--text-tertiary);font-size:10px}
    .platform-website{font-size:13px;color:var(--game-accent);transition:opacity .15s;display:flex;align-items:center;gap:4px}
    .platform-website:hover{opacity:.7}
    .rate-console-btn{margin-top:12px;display:inline-flex;align-items:center;gap:6px;background:var(--game-accent);color:white;border:none;border-radius:20px;padding:7px 16px;font-size:12px;font-weight:500;font-family:inherit;cursor:pointer;transition:opacity .15s}
    .rate-console-btn:hover{opacity:.85}

    .stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:28px}
    .stat-card{background:var(--bg-card);border:1px solid var(--border);border-radius:14px;padding:20px 18px 16px}
    .stat-card.score-card-great{border-bottom:2px solid var(--score-great)}
    .stat-card.score-card-good{border-bottom:2px solid var(--score-good)}
    .stat-card.score-card-mid{border-bottom:2px solid var(--score-mid)}
    .stat-card.score-card-low{border-bottom:2px solid var(--score-low)}
    .stat-num{font-family:'Century Schoolbook','New Century Schoolbook',Georgia,serif;font-size:32px;font-weight:400;line-height:1;letter-spacing:-1px;margin-bottom:5px;color:var(--text-primary);background:none}
    .stat-num.great{color:var(--score-great)}
    .stat-num.good{color:var(--score-good)}
    .stat-num.mid{color:var(--score-mid)}
    .stat-num.low{color:var(--score-low)}
    .stat-label{font-size:10px;font-weight:500;color:var(--text-tertiary);text-transform:uppercase;letter-spacing:.08em}
    .stat-sub{font-size:10px;color:var(--text-tertiary);margin-top:2px}

    .about-grid{display:grid;grid-template-columns:1fr 260px;gap:24px;margin-bottom:40px}
    @media(max-width:720px){.about-grid{grid-template-columns:1fr}}
    .description{font-size:14px;line-height:1.8;color:var(--text-secondary)}
    .description.collapsed{display:-webkit-box;-webkit-line-clamp:5;-webkit-box-orient:vertical;overflow:hidden}
    .read-more-btn{background:none;border:none;font-family:inherit;font-size:12px;color:var(--game-accent);cursor:pointer;padding:8px 0 0;display:block}
    .details-card{background:var(--bg-card);border:1px solid var(--border);border-radius:14px;padding:4px 18px;height:fit-content}
    .detail-row{display:flex;gap:12px;padding:11px 0;border-bottom:1px solid var(--border-subtle);font-size:12px}
    .detail-row:last-child{border-bottom:none}
    .detail-label{flex-shrink:0;width:80px;color:var(--text-tertiary);padding-top:2px;text-transform:uppercase;letter-spacing:.06em;font-size:10px;font-weight:500}
    .detail-value{color:var(--text-secondary);line-height:1.5}
    .detail-link{color:var(--game-accent);transition:opacity .15s}
    .detail-link:hover{opacity:.7}

    .section{margin-bottom:48px}
    .section-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:16px}
    .section-title{font-size:11px;font-weight:600;text-transform:uppercase;letter-spacing:.12em;color:var(--text-tertiary)}
    .section-count{font-size:12px;color:var(--text-tertiary)}

    .game-row{display:grid;grid-template-columns:repeat(5,1fr);gap:14px}
    @media(max-width:700px){.game-row{grid-template-columns:repeat(3,1fr)}}
    .row-nav{display:flex;align-items:center;gap:8px;margin-top:14px}
    .row-nav-info{font-size:12px;color:var(--text-tertiary)}
    .row-arrow{background:var(--bg-card);border:1px solid var(--border);border-radius:8px;width:32px;height:32px;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .15s;color:var(--text-secondary);flex-shrink:0}
    .row-arrow:hover{border-color:var(--game-accent);color:var(--game-accent)}
    .row-arrow:disabled{opacity:.25;cursor:default;pointer-events:none}

    .pill-strip-wrap{position:relative;display:flex;align-items:center;gap:6px;margin-bottom:14px}
    .strip-arrow{background:var(--bg-card);border:1px solid var(--border);border-radius:8px;width:28px;height:28px;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .15s;color:var(--text-secondary);flex-shrink:0}
    .strip-arrow:hover{border-color:var(--game-accent);color:var(--game-accent)}
    .strip-arrow:disabled{opacity:.2;cursor:default;pointer-events:none}
    .year-pill-strip{display:flex;gap:6px;overflow-x:auto;padding-bottom:4px;scroll-snap-type:x mandatory;-webkit-overflow-scrolling:touch;scrollbar-width:none;flex:1;min-width:0}
    .year-pill-strip::-webkit-scrollbar{display:none}
    .year-pill{display:flex;align-items:center;gap:5px;padding:5px 10px;border-radius:20px;border:1px solid var(--border);background:var(--bg-card);cursor:pointer;white-space:nowrap;scroll-snap-align:start;transition:border-color .15s,background .15s;font-family:inherit}
    .year-pill:hover{border-color:rgba(var(--accent-rgb),.4)}
    .year-pill.active{border-color:var(--game-accent);background:rgba(var(--accent-rgb),.08)}
    .year-pill-dot{width:7px;height:7px;border-radius:50%;flex-shrink:0}
    .year-pill-label{font-size:12px;font-weight:600;color:var(--text-primary)}
    .year-pill-count{font-size:11px;color:var(--text-tertiary)}
    .rank-badge{position:absolute;top:6px;left:6px;background:rgba(0,0,0,.6);color:#fff;font-size:10px;font-weight:700;padding:2px 6px;border-radius:4px;line-height:1.4;letter-spacing:.01em;backdrop-filter:blur(4px)}

    .year-panel-header{display:flex;align-items:center;gap:12px;margin-bottom:14px;padding-bottom:10px;border-bottom:1px solid var(--border)}
    .year-panel-label{font-family:'DM Serif Display',serif;font-size:22px;letter-spacing:-.3px;color:var(--text-primary)}
    .year-panel-count{font-size:12px;color:var(--text-tertiary)}

    .game-card{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;overflow:hidden;transition:transform .15s,box-shadow .15s,border-color .15s;display:block;text-decoration:none;position:relative}
    .game-card:hover{transform:translateY(-3px);box-shadow:var(--shadow-md);border-color:rgba(var(--accent-rgb),.3)}
    .game-cover{aspect-ratio:3/4;background:var(--tag-bg);overflow:hidden;position:relative;width:100%}
    .game-cover img{width:100%;height:100%;object-fit:cover;display:block}
    .game-cover-placeholder{width:100%;height:100%;position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-size:32px}
    .game-score-badge{position:absolute;top:7px;right:7px;width:32px;height:32px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;font-family:'Century Schoolbook',Georgia,serif}
    .badge-perfect{color:#09090a;background:#ffffff;border:1px solid rgba(255,255,255,.8);box-shadow:0 0 10px rgba(255,255,255,.9),0 0 24px rgba(255,255,255,.6),0 0 48px rgba(255,255,255,.3)}
    [data-theme="light"] .badge-perfect{color:#ffffff;background:#1a1a2e;border:1px solid rgba(30,26,60,.3);box-shadow:0 0 8px rgba(30,26,60,.4)}
    .badge-great  {color:var(--score-great);  background:var(--score-great-bg);  border:1px solid rgba(74,222,128,.3)}
    .badge-teal   {color:var(--score-teal);   background:var(--score-teal-bg);   border:1px solid rgba(45,212,191,.3)}
    .badge-good   {color:var(--score-good);   background:var(--score-good-bg);   border:1px solid rgba(96,165,250,.3)}
    .badge-mid    {color:var(--score-mid);    background:var(--score-mid-bg);    border:1px solid rgba(251,191,36,.3)}
    .badge-orange {color:var(--score-orange); background:var(--score-orange-bg); border:1px solid rgba(251,146,60,.3)}
    .badge-low    {color:var(--score-low);    background:var(--score-low-bg);    border:1px solid rgba(248,113,113,.3)}
    .exclusive-badge{position:absolute;top:7px;left:7px;background:var(--game-accent);color:white;font-size:8px;font-weight:700;padding:2px 5px;border-radius:4px;text-transform:uppercase;letter-spacing:.06em}
    .game-info{padding:9px 10px 11px}
    .game-title-text{font-size:11px;font-weight:500;line-height:1.35;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;margin-bottom:2px}
    .game-review-count{font-size:10px;color:var(--text-tertiary);margin-top:1px}

    .reviews-toolbar{display:flex;align-items:center;gap:6px;margin-bottom:20px}
    .sort-tab{background:var(--tag-bg);color:var(--text-tertiary);border:1px solid var(--border);border-radius:20px;padding:5px 14px;font-size:11px;font-weight:500;font-family:inherit;cursor:pointer;transition:all .15s;letter-spacing:.04em;text-transform:uppercase}
    .sort-tab.active{background:var(--game-accent);color:white;border-color:transparent}
    .review-card{padding:22px 0;border-bottom:1px solid var(--border-subtle)}
    .review-card:last-child{border-bottom:none}
    .review-top{display:flex;align-items:center;gap:10px;margin-bottom:12px}
    .avatar{width:34px;height:34px;border-radius:50%;background:var(--bg-hover);border:1px solid var(--border);flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:600;color:var(--text-tertiary);overflow:hidden}
    .avatar img{width:100%;height:100%;object-fit:cover}
    .reviewer-info{flex:1;min-width:0}
    .reviewer-name{font-size:13px;font-weight:500;transition:color .15s}
    .reviewer-name:hover{color:var(--text-secondary)}
    .reviewer-meta{font-size:11px;color:var(--text-tertiary);display:flex;gap:6px;align-items:center;flex-wrap:wrap;margin-top:2px}
    .score-badge{flex-shrink:0;width:36px;height:36px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:14px;font-weight:700;font-family:'Century Schoolbook',Georgia,serif}
    .score-perfect{color:#09090a!important;background:#ffffff!important;border:1px solid rgba(255,255,255,.8)!important;box-shadow:0 0 10px rgba(255,255,255,.7),0 0 20px rgba(255,255,255,.3)}
    [data-theme="light"] .score-perfect{color:#ffffff!important;background:#1a1a2e!important;border:1px solid rgba(30,26,60,.3)!important;box-shadow:0 0 8px rgba(30,26,60,.4)}
    .score-great  {color:var(--score-great);  background:var(--score-great-bg);  border:1px solid rgba(74,222,128,.25)}
    .score-teal   {color:var(--score-teal);   background:var(--score-teal-bg);   border:1px solid rgba(45,212,191,.25)}
    .score-good   {color:var(--score-good);   background:var(--score-good-bg);   border:1px solid rgba(96,165,250,.25)}
    .score-mid    {color:var(--score-mid);    background:var(--score-mid-bg);    border:1px solid rgba(251,191,36,.25)}
    .score-orange {color:var(--score-orange); background:var(--score-orange-bg); border:1px solid rgba(251,146,60,.25)}
    .score-low    {color:var(--score-low);    background:var(--score-low-bg);    border:1px solid rgba(248,113,113,.25)}
    .review-title{font-family:'DM Serif Display',serif;font-size:18px;line-height:1.3;margin-bottom:8px;letter-spacing:-.2px}
    .review-body{font-size:14px;line-height:1.75;color:var(--text-secondary)}
    .review-body.collapsed{display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}
    .review-footer{display:flex;align-items:center;gap:8px;margin-top:12px;flex-wrap:wrap}
    .expand-btn{font-size:11px;color:var(--game-accent);margin-left:auto;cursor:pointer;background:none;border:none;font-family:inherit;transition:opacity .15s;padding:0;font-weight:500}
    .expand-btn:hover{opacity:.7}
    .empty-state{padding:60px 0;text-align:center;color:var(--text-tertiary)}
    .empty-title{font-family:'DM Serif Display',serif;font-size:20px;color:var(--text-secondary);margin-bottom:8px}
    .write-review-btn{display:inline-block;margin-top:16px;background:var(--game-accent);color:white;border-radius:20px;padding:9px 20px;font-size:14px;font-weight:500;border:none;cursor:pointer;font-family:inherit;transition:opacity .15s}
    .write-review-btn:hover{opacity:.85}

    .tab-nav{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:28px}
    .tab-link{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;padding:16px 12px;background:var(--bg-card);border:1px solid var(--border);border-radius:14px;transition:all .2s;position:relative;overflow:hidden;text-decoration:none}
    .tab-link:hover{border-color:rgba(var(--accent-rgb),.35);transform:translateY(-2px);box-shadow:var(--shadow-md)}
    .tab-link.active{border-color:var(--game-accent);box-shadow:0 0 0 1px var(--game-accent),0 4px 20px rgba(var(--accent-rgb),.15)}
    .tab-link.active::after{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:var(--game-accent)}
    .tab-icon{width:30px;height:30px;border-radius:8px;background:var(--tag-bg);display:flex;align-items:center;justify-content:center;transition:background .2s;flex-shrink:0}
    .tab-link.active .tab-icon svg,.tab-link:hover .tab-icon svg{stroke:var(--game-accent)}
    .tab-label{font-size:13px;font-weight:600;color:var(--text-secondary);transition:color .2s}
    .tab-link.active .tab-label,.tab-link:hover .tab-label{color:var(--text-primary)}
    .tab-meta{font-size:11px;color:var(--text-tertiary)}
    .tab-link.active .tab-meta{color:var(--game-accent)}
    .tab-count-badge{background:var(--game-accent);color:white;font-size:10px;font-weight:700;padding:1px 7px;border-radius:10px;position:absolute;top:8px;right:8px;z-index:2}
    .tab-panel{display:none}
    .tab-panel.active{display:block}

    @media(max-width:600px){
      .platform-header{flex-direction:column;align-items:flex-start}
      .platform-header.has-banner{margin-top:-40px}
      .platform-header.no-banner{margin-top:20px}
      .platform-name{font-size:26px}
      .hero{height:200px}
      .stats-row{grid-template-columns:repeat(2,1fr);gap:8px}
      .stat-num{font-size:28px}
      .jump-nav{display:none}
    }
    /* ── Mobile bottom nav ── */
    .mobile-nav{display:none}
    @media(max-width:768px){
      body{padding-bottom:calc(56px + env(safe-area-inset-bottom,0px))}
      .mobile-nav{display:flex;position:fixed;bottom:0;left:0;right:0;height:calc(56px + env(safe-area-inset-bottom,0px));padding-bottom:env(safe-area-inset-bottom,0px);background:var(--bg-sidebar);border-top:1px solid var(--border);z-index:100;align-items:stretch}
      .mn-item{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;text-decoration:none;color:var(--text-tertiary);transition:color 0.15s;padding:6px 2px;min-width:0;-webkit-tap-highlight-color:transparent}
      .mn-item.active{color:var(--text-primary)}
      .mn-item:active{opacity:.65}
      .mn-icon{width:22px;height:22px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
      .mn-label{font-size:9px;font-weight:500;letter-spacing:.02em;white-space:nowrap}
    }
  </style>
</head>

<body>
  <nav class="topnav">
    <button class="back-btn" onclick="(function(){var r=document.referrer;try{var u=new URL(r);if(u.hostname===location.hostname&&u.pathname!==location.pathname){window.location.assign(r);return}}catch(e){}window.location.href='/'})()">
      <svg width="15" height="15" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10 12L6 8L10 4"/></svg>
      Back
    </button>
    <a href="/" class="logo"><span class="logo-dot"></span>Chekpoint</a>
    <button class="theme-btn" onclick="toggleTheme()">
      <svg id="themeIcon" width="17" height="17" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="9" cy="9" r="3.5"/>
        <path d="M9 1v2M9 15v2M1 9h2M15 9h2M3.22 3.22l1.42 1.42M13.36 13.36l1.42 1.42M13.36 4.64l1.42-1.42M3.22 14.78l1.42-1.42"/>
      </svg>
    </button>
  </nav>

  {(hasMosaic || bannerImage) && (
    <div class="hero">
      {hasMosaic
        ? (<div class="hero-mosaic visible" id="heroMosaic">
            {mosaicTiles.map((url: string) => (
              <div class="hero-mosaic-tile" style={`background-image:url('${url}')`}></div>
            ))}
          </div>)
        : (<img class="hero-img" src={bannerImage!} alt={platform.name} />)
      }
      <div class="hero-side-fade"></div>
      <div class="hero-overlay"></div>
    </div>
  )}

  <div class="page">

    <div class={`platform-header ${(hasMosaic || bannerImage) ? 'has-banner' : 'no-banner'}`}>
      <div class={`platform-logo-wrap${platform.logo_url ? ' custom' : ''}`}>
        {platform.logo_url
          ? <img class="platform-logo custom" src={platform.logo_url} alt={platform.name} />
          : igdbData?.platform_logo?.url
            ? <img class="platform-logo" src={igdbImage(igdbData.platform_logo.url, 't_logo_med') ?? ''} alt={platform.name} />
            : <span>🎮</span>
        }
      </div>
      <div class="platform-info">
        <div class="platform-name-row">
          <h1 class="platform-name">{platform.name}</h1>
          {consoleAvg && (
            <div class="console-score-wrap">
              <span class={`console-score-num ${consoleScoreClass}`}>{consoleAvg}</span>
              <span class="console-score-label">· {consoleTotalRatings.toLocaleString()} ratings</span>
            </div>
          )}
        </div>
        <div class="platform-meta">
          {releaseYearDisplay && <span class="platform-meta-item">Released {releaseYearDisplay}</span>}
          {generation && (
            <>
              {releaseYearDisplay && <span class="platform-meta-sep">•</span>}
              <span class="platform-meta-item">{generation} Generation</span>
            </>
          )}
          {family && (
            <>
              <span class="platform-meta-sep">•</span>
              <span class="platform-meta-item">{family}</span>
            </>
          )}
          {officialSite && (
            <>
              <span class="platform-meta-sep">•</span>
              <a href={officialSite} target="_blank" rel="noopener" class="platform-website">
                <svg width="12" height="12" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="8" r="6"/><path d="M8 2c0 0-3 2-3 6s3 6 3 6M8 2c0 0 3 2 3 6s-3 6-3 6M2 8h12"/></svg>
                Website
              </a>
            </>
          )}
        </div>
        <button class="rate-console-btn" onclick="openRateModal()">
          <svg width="13" height="13" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M8 2v12M2 8h12"/></svg>
          Rate this console
        </button>
      </div>
    </div>

    <div class="stats-row">
      <div class="stat-card">
        <div class="stat-num">{displayGameCount.toLocaleString()}</div>
        <div class="stat-label">Games</div>
      </div>
      <div class="stat-card">
        <div class="stat-num">{totalGameReviews.toLocaleString()}</div>
        <div class="stat-label">Game reviews</div>
        <div class="stat-sub">across all games</div>
      </div>
      <div class={`stat-card ${gameAvgScoreClass ? `score-card-${gameAvgScoreClass}` : ''}`}>
        {gameAvg
          ? <div class={`stat-num ${gameAvgScoreClass}`}>{gameAvg}</div>
          : <div class="stat-num" style="color:var(--text-tertiary)">—</div>
        }
        <div class="stat-label">Avg game score</div>
      </div>
      <div class={`stat-card ${consoleScoreClass ? `score-card-${consoleScoreClass}` : ''}`}>
        {consoleAvg
          ? <div class={`stat-num ${consoleScoreClass}`}>{consoleAvg}</div>
          : <div class="stat-num" style="color:var(--text-tertiary)">—</div>
        }
        <div class="stat-label">Console score</div>
        {consoleTotalRatings > 0 && <div class="stat-sub">{consoleTotalRatings.toLocaleString()} ratings</div>}
      </div>
    </div>

    <!-- TAB NAV -->
    <nav class="tab-nav">
      <a href={`/platforms/${platform.slug}?tab=overview`} class={`tab-link ${activeTab === 'overview' ? 'active' : ''}`}>
        <div class="tab-icon">
          <svg width="15" height="15" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="2" width="6" height="6" rx="1.5"/><rect x="10" y="2" width="6" height="6" rx="1.5"/>
            <rect x="2" y="10" width="6" height="6" rx="1.5"/><rect x="10" y="10" width="6" height="6" rx="1.5"/>
          </svg>
        </div>
        <div class="tab-label">Overview</div>
        <div class="tab-meta">Info &amp; games by year</div>
      </a>
      <a href={`/platforms/${platform.slug}?tab=reviews`} class={`tab-link ${activeTab === 'reviews' ? 'active' : ''}`}>
        {platformReviewList.length > 0 && <span class="tab-count-badge">{platformReviewList.length}</span>}
        <div class="tab-icon">
          <svg width="15" height="15" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 4h12M3 8h8M3 12h5"/>
          </svg>
        </div>
        <div class="tab-label">Reviews</div>
        <div class="tab-meta">{platformReviewList.length > 0 ? `${platformReviewList.length} ${platformReviewList.length === 1 ? 'review' : 'reviews'}` : 'No reviews yet'}</div>
      </a>
      <a href={`/platforms/${platform.slug}?tab=stats`} class={`tab-link ${activeTab === 'stats' ? 'active' : ''}`}>
        <div class="tab-icon">
          <svg width="15" height="15" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 14l4-5 3 3 3-4 4 6"/>
          </svg>
        </div>
        <div class="tab-label">Stats</div>
        <div class="tab-meta">Library insights</div>
      </a>
    </nav>

    <!-- OVERVIEW TAB -->
    <div class={`tab-panel ${activeTab === 'overview' ? 'active' : ''}`}>
      {summary && (
        <div class="about-grid">
          <div>
            <p class="description collapsed" id="desc">{summary}</p>
            <button class="read-more-btn" id="readMoreBtn" onclick="toggleDesc()">Read more →</button>
          </div>
          <div class="details-card">
            {releaseDate && releaseDate !== 'N/A' && (
              <div class="detail-row">
                <span class="detail-label">Released</span>
                <span class="detail-value">{releaseDate}</span>
              </div>
            )}
            {generation && (
              <div class="detail-row">
                <span class="detail-label">Generation</span>
                <span class="detail-value">{generation}</span>
              </div>
            )}
            {family && (
              <div class="detail-row">
                <span class="detail-label">Family</span>
                <span class="detail-value">{family}</span>
              </div>
            )}
            <div class="detail-row">
              <span class="detail-label">Games</span>
              <span class="detail-value">{displayGameCount.toLocaleString()} on Chekpoint</span>
            </div>
            {totalGameReviews > 0 && (
              <div class="detail-row">
                <span class="detail-label">Reviews</span>
                <span class="detail-value">{totalGameReviews.toLocaleString()} total</span>
              </div>
            )}
            {officialSite && (
              <div class="detail-row">
                <span class="detail-label">Website</span>
                <a href={officialSite} target="_blank" rel="noopener" class="detail-value detail-link">
                  {officialSite.replace(/^https?:\/\//, '').replace(/\/$/, '')}
                </a>
              </div>
            )}
          </div>
        </div>
      )}

      {allGames.length > 0 && (
        <div class="section">
          <div class="section-header">
            <span class="section-title">Top Rated</span>
            <span class="section-count">{topRated.length} games</span>
          </div>
          <div class="game-row" id="topRatedRow"></div>
          <div class="row-nav">
            <button class="row-arrow" id="topPrev" onclick="pageRow('top',-1)">
              <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 12L6 8l4-4"/></svg>
            </button>
            <button class="row-arrow" id="topNext" onclick="pageRow('top',1)">
              <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 12l4-4-4-4"/></svg>
            </button>
            <span class="row-nav-info" id="topInfo"></span>
          </div>
        </div>
      )}

      {years.length > 0 && (
        <div class="section">
          <div class="section-header">
            <span class="section-title">Games by Year</span>
            <span class="section-count" id="yearPanelCount"></span>
          </div>
          <div class="pill-strip-wrap">
            <button class="strip-arrow" id="yearStripPrev" onclick="scrollYearStrip(-1)" disabled>
              <svg width="12" height="12" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10 12L6 8l4-4"/></svg>
            </button>
            <div class="year-pill-strip" id="yearPillStrip"></div>
            <button class="strip-arrow" id="yearStripNext" onclick="scrollYearStrip(1)">
              <svg width="12" height="12" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M6 12l4-4-4-4"/></svg>
            </button>
          </div>
          <div>
            <div class="year-panel-header">
              <span class="year-panel-label" id="yearPanelLabel"></span>
              <span class="year-panel-count" id="yearPanelTotal"></span>
            </div>
            <div class="game-row" id="yearGamesRow"></div>
            <div class="row-nav">
              <button class="row-arrow" id="yearPrev" onclick="pageRow('year',-1)">
                <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 12L6 8l4-4"/></svg>
              </button>
              <button class="row-arrow" id="yearNext" onclick="pageRow('year',1)">
                <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 12l4-4-4-4"/></svg>
              </button>
              <span class="row-nav-info" id="yearInfo"></span>
            </div>
          </div>
        </div>
      )}
    </div>

    <!-- REVIEWS TAB -->
    <div class={`tab-panel ${activeTab === 'reviews' ? 'active' : ''}`}>
      <div class="section-header" style="margin-bottom:20px">
        <span class="section-title">Console Reviews</span>
        <span class="section-count">
          {platformReviewList.length} {platformReviewList.length === 1 ? 'review' : 'reviews'}
        </span>
      </div>

      {platformReviewList.length === 0 ? (
        <div class="empty-state">
          <div class="empty-title">No reviews yet</div>
          <p>Be the first to review the {platform.name}.</p>
          <button class="write-review-btn" onclick="openRateModal()">+ Write a review</button>
        </div>
      ) : (
        <>
          <div class="reviews-toolbar">
            <button class="sort-tab active" onclick="sortReviews('recent',this)">Recent</button>
            <button class="sort-tab" onclick="sortReviews('highest',this)">Highest</button>
            <button class="sort-tab" onclick="sortReviews('lowest',this)">Lowest</button>
          </div>
          <div id="platformReviewsList">
            {platformReviewList.map((review: any) => (
              <div class="review-card" data-score={review.score} data-date={review.created_at}>
                <div class="review-top">
                  <div class="avatar">
                    {review.profiles?.avatar_url
                      ? <img src={review.profiles.avatar_url} alt={review.profiles.username} />
                      : getInitials(review.profiles?.username ?? '??')
                    }
                  </div>
                  <div class="reviewer-info">
                    <a href={`/reviewers/${review.profiles?.username}`} class="reviewer-name">
                      @{review.profiles?.username ?? 'unknown'}
                    </a>
                    <div class="reviewer-meta">
                      <span>{timeAgo(review.created_at)}</span>
                    </div>
                  </div>
                  <div class={`score-badge score-${scoreClass(review.score)}`}>{review.score}</div>
                </div>
                {review.title && <div class="review-title">{review.title}</div>}
                {review.body && (
                  <>
                    <div class="review-body collapsed" id={`prb-${review.id}`}>{review.body}</div>
                    <div class="review-footer">
                      <button class="expand-btn" onclick={`toggleReview('${review.id}',this)`}>Read more →</button>
                    </div>
                  </>
                )}
              </div>
            ))}
          </div>
        </>
      )}
    </div>

    <!-- STATS TAB -->
    <div class={`tab-panel ${activeTab === 'stats' ? 'active' : ''}`}>
      {totalGameReviews === 0 ? (
        <div class="empty-state">
          <div class="empty-title">No stats yet</div>
          <p>Stats will appear once games are reviewed.</p>
        </div>
      ) : (
        <div id="platformStatsContent"></div>
      )}
    </div>

  </div>

  <script is:inline define:vars={{ topRatedJson, byYearJson, yearsJson, platformStatsJson, activeTab, yearGameCountsJson, yearAvgScoresJson, accentLight, accentDark, accentRgb }}>

    var topRated = JSON.parse(topRatedJson);
    var byYear = JSON.parse(byYearJson);
    var years = JSON.parse(yearsJson);

    var scoreBorderColors = {'badge-perfect':'#ffffff','badge-great':'#4ade80','badge-teal':'#2dd4bf','badge-good':'#60a5fa','badge-mid':'#fbbf24','badge-orange':'#fb923c','badge-low':'#f87171'}

    function makeCard(g, isExc, rank) {
      var cv = g.cover ? '<img src="'+g.cover+'" alt="" loading="lazy"/>' : '<div class="game-cover-placeholder">🎮</div>';
      var bd = g.score ? '<div class="game-score-badge '+g.badge+'">'+g.score+'</div>' : '';
      var ex = isExc ? '<div class="exclusive-badge">Exclusive</div>' : '';
      var rk = rank ? '<div class="rank-badge">#'+rank+'</div>' : '';
      var borderBottom = g.badge && scoreBorderColors[g.badge] ? 'border-bottom:2px solid '+scoreBorderColors[g.badge]+';' : '';
      return '<a href="/games/'+g.slug+'" class="game-card" style="'+borderBottom+'">'
        +'<div class="game-cover">'+cv+ex+rk+'</div>'
        +bd
        +'<div class="game-info">'
        +'<div class="game-title-text">'+g.title+'</div>'
        +(g.reviews?'<div class="game-review-count">'+g.reviews+'</div>':'')
        +'</div></a>';
    }

    var rows = {};

    function initRow(key, data, rowId, prevId, nextId, infoId, isExc, showRank) {
      rows[key] = {data:data,page:0,pageSize:5,rowId:rowId,prevId:prevId,nextId:nextId,infoId:infoId,isExc:isExc||false,showRank:showRank||false};
      renderRow(key);
    }

    function renderRow(key) {
      var r = rows[key];
      var start = r.page * r.pageSize;
      var slice = r.data.slice(start, start+r.pageSize);
      var el = document.getElementById(r.rowId);
      if (el) el.innerHTML = slice.map(function(g,i){return makeCard(g,r.isExc,r.showRank?(start+i+1):null);}).join('');
      var tot = Math.ceil(r.data.length / r.pageSize);
      var prev = document.getElementById(r.prevId);
      var next = document.getElementById(r.nextId);
      var info = document.getElementById(r.infoId);
      if (prev) prev.disabled = r.page === 0;
      if (next) next.disabled = r.page >= tot-1;
      if (info) info.textContent = (r.page+1)+' / '+tot;
    }

    function pageRow(key, dir) {
      var r = rows[key];
      if (!r) return;
      var tot = Math.ceil(r.data.length/r.pageSize);
      r.page = Math.max(0, Math.min(tot-1, r.page+dir));
      renderRow(key);
    }
    window.pageRow = pageRow;

    function scrollYearStrip(dir) {
      var strip = document.getElementById('yearPillStrip');
      if (!strip) return;
      strip.scrollBy({ left: dir * 200, behavior: 'smooth' });
    }
    window.scrollYearStrip = scrollYearStrip;

    function updateStripArrows() {
      var strip = document.getElementById('yearPillStrip');
      var prev = document.getElementById('yearStripPrev');
      var next = document.getElementById('yearStripNext');
      if (!strip || !prev || !next) return;
      prev.disabled = strip.scrollLeft <= 0;
      next.disabled = strip.scrollLeft + strip.clientWidth >= strip.scrollWidth - 1;
    }

    if (topRated.length > 0) {
      initRow('top', topRated, 'topRatedRow', 'topPrev', 'topNext', 'topInfo', false, true);
    }

    if (years.length > 0) {
      var activeYear = years[0];
      var yearGameCounts = JSON.parse(yearGameCountsJson);
      var yearAvgScores = JSON.parse(yearAvgScoresJson);
      var yearPills = {};

      var strip = document.getElementById('yearPillStrip');
      if (strip) {
        years.forEach(function(y) {
          var avg = yearAvgScores[y];
          var dot = avg != null ? (avg >= 8 ? '#22c55e' : avg >= 6 ? '#3b82f6' : avg >= 4 ? '#f59e0b' : '#ef4444') : '#71717a';
          var pill = document.createElement('button');
          pill.className = 'year-pill';
          pill.innerHTML = '<span class="year-pill-dot" style="background:' + dot + '"></span>'
            + '<span class="year-pill-label">' + y + '</span>'
            + '<span class="year-pill-count">' + (yearGameCounts[y] || 0) + '</span>';
          pill.addEventListener('click', function(){ selectYear(y); });
          yearPills[y] = pill;
          strip.appendChild(pill);
        });
        strip.addEventListener('scroll', updateStripArrows);
        updateStripArrows();
      }

      function selectYear(y) {
        activeYear = y;
        Object.keys(yearPills).forEach(function(yr) {
          yearPills[yr].classList.toggle('active', parseInt(yr) === y);
        });
        if (strip && yearPills[y]) {
          var pill = yearPills[y];
          var target = pill.offsetLeft - (strip.clientWidth / 2) + (pill.offsetWidth / 2);
          strip.scrollTo({left: Math.max(0, target), behavior: 'smooth'});
        }
        var games = byYear[y] || [];
        var totalCount = yearGameCounts[y] || games.length;
        var lbl = document.getElementById('yearPanelLabel');
        var tot = document.getElementById('yearPanelTotal');
        var cnt = document.getElementById('yearPanelCount');
        if (lbl) lbl.textContent = y;
        if (tot) tot.textContent = totalCount + ' games';
        if (cnt) cnt.textContent = totalCount + ' games';
        rows['year'] = {data:games,page:0,pageSize:5,rowId:'yearGamesRow',prevId:'yearPrev',nextId:'yearNext',infoId:'yearInfo',isExc:false};
        renderRow('year');
      }
      selectYear(activeYear);
    }

    function sortReviews(type, btn) {
      document.querySelectorAll('.sort-tab').forEach(function(b){b.classList.remove('active');});
      btn.classList.add('active');
      var list = document.getElementById('platformReviewsList');
      if (!list) return;
      var cards = Array.from(list.querySelectorAll('.review-card'));
      cards.sort(function(a,b) {
        if (type === 'highest') return parseInt(b.dataset.score) - parseInt(a.dataset.score);
        if (type === 'lowest') return parseInt(a.dataset.score) - parseInt(b.dataset.score);
        return new Date(b.dataset.date).getTime() - new Date(a.dataset.date).getTime();
      });
      cards.forEach(function(card){list.appendChild(card);});
    }
    window.sortReviews = sortReviews;

    function toggleReview(id, btn) {
      var b = document.getElementById('prb-'+id);
      if (!b) return;
      b.classList.toggle('collapsed');
      btn.textContent = b.classList.contains('collapsed') ? 'Read more →' : 'Read less ↑';
    }
    window.toggleReview = toggleReview;

    function toggleDesc() {
      var d=document.getElementById('desc'), b=document.getElementById('readMoreBtn');
      if (!d||!b) return;
      d.classList.toggle('collapsed');
      b.textContent = d.classList.contains('collapsed') ? 'Read more →' : 'Read less ↑';
    }
    window.toggleDesc = toggleDesc;

    function openRateModal() {
      alert('Sign in to rate this console');
    }
    window.openRateModal = openRateModal;

    // ── PLATFORM STATS ──
    var ps = JSON.parse(platformStatsJson)

    function buildPlatformStats() {
      var el = document.getElementById('platformStatsContent')
      if (!el) return
      var html = ''

      // hero card: avg game score
      if (ps.gameAvg) {
        var sColor = ps.gameAvg >= 9 ? '#4ade80' : ps.gameAvg >= 7 ? '#60a5fa' : ps.gameAvg >= 5 ? '#fbbf24' : '#f87171'
        html += '<div style="background:var(--bg-card);border:1px solid ' + sColor + '44;border-radius:12px;padding:16px 20px;position:relative;overflow:hidden;display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:8px">'
        html += '<div style="position:absolute;inset:0;background:linear-gradient(to right,' + sColor + '10 0%,transparent 55%);pointer-events:none"></div>'
        html += '<div style="position:relative">'
        html += '<div style="font-size:11px;font-weight:700;color:' + sColor + ';text-transform:uppercase;letter-spacing:.1em;margin-bottom:3px;opacity:.85">Avg game score</div>'
        html += '<div style="font-size:11px;color:var(--text-tertiary)">across all reviewed games</div>'
        html += '</div>'
        html += '<div style="font-family:\'DM Serif Display\',serif;font-size:52px;letter-spacing:-2px;line-height:1;color:' + sColor + ';position:relative;flex-shrink:0">' + ps.gameAvg.toFixed(1) + '</div>'
        html += '</div>'
      }

      // 3-col mini stats
      var cColor = ps.consoleAvg ? (ps.consoleAvg >= 9 ? '#4ade80' : ps.consoleAvg >= 7 ? '#60a5fa' : ps.consoleAvg >= 5 ? '#fbbf24' : '#f87171') : '#484644'
      html += '<div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:10px">'
      var mini = [
        { lbl: 'Total reviews', val: ps.totalGameReviews.toLocaleString(), sub: 'across all games', color: '#6050c8' },
        { lbl: 'Reviewed games', val: ps.reviewedGameCount.toLocaleString(), sub: 'of ' + ps.gameCount.toLocaleString() + ' total', color: '#60a5fa' },
        { lbl: 'Console score', val: ps.consoleAvg ? ps.consoleAvg.toFixed(1) : '—', sub: ps.consoleTotalRatings > 0 ? ps.consoleTotalRatings + ' ratings' : 'no ratings yet', color: cColor },
      ]
      mini.forEach(function(sc) {
        html += '<div style="background:var(--bg-card);border:1px solid ' + sc.color + '33;border-radius:12px;padding:12px 14px;position:relative;overflow:hidden">'
        html += '<div style="position:absolute;inset:0;background:linear-gradient(135deg,' + sc.color + '0d 0%,transparent 70%);pointer-events:none"></div>'
        html += '<div style="font-size:10px;font-weight:700;color:' + sc.color + ';text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px;opacity:.85;position:relative">' + sc.lbl + '</div>'
        html += '<div style="font-family:\'DM Serif Display\',serif;font-size:32px;letter-spacing:-.5px;line-height:1;color:' + sc.color + ';position:relative;margin-bottom:3px">' + sc.val + '</div>'
        html += '<div style="font-size:10px;color:var(--text-tertiary);position:relative">' + sc.sub + '</div>'
        html += '</div>'
      })
      html += '</div>'

      // score distribution + reviews by year side by side
      var hasDist = ps.gameScoreDist && ps.gameScoreDist.some(function(v) { return v > 0 })
      var hasYear = ps.reviewsByYearData && ps.reviewsByYearData.length > 0
      if (hasDist || hasYear) {
        var cols = (hasDist && hasYear) ? '3fr 2fr' : '1fr'
        html += '<div style="display:grid;grid-template-columns:' + cols + ';gap:8px;margin-bottom:10px">'
        if (hasDist) {
          var maxDist = Math.max.apply(null, ps.gameScoreDist.concat([1]))
          var barColors = ['#f87171','#f87171','#fb923c','#fb923c','#fbbf24','#fbbf24','#60a5fa','#2dd4bf','#4ade80','#4ade80']
          html += '<div style="background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:12px 14px">'
          html += '<div style="font-size:9px;font-weight:700;color:var(--text-tertiary);text-transform:uppercase;letter-spacing:.12em;margin-bottom:10px">Score distribution — per-game avg</div>'
          html += '<div style="display:flex;gap:4px;align-items:flex-end;height:60px">'
          ps.gameScoreDist.forEach(function(count, i) {
            var h = Math.max(4, (count / maxDist) * 52)
            html += '<div style="flex:1;display:flex;flex-direction:column;align-items:center;gap:3px" title="~' + (i+1) + '/10: ' + count + ' games">'
            html += '<div style="width:100%;border-radius:4px 4px 0 0;height:' + h + 'px;background:' + barColors[i] + ';opacity:.7;min-height:4px"></div>'
            html += '<div style="font-size:9px;color:var(--text-tertiary);font-weight:500">' + (i+1) + '</div>'
            html += '</div>'
          })
          html += '</div></div>'
        }
        if (hasYear) {
          var maxYear = Math.max.apply(null, ps.reviewsByYearData.map(function(d) { return d.count }).concat([1]))
          html += '<div style="background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:12px 14px">'
          html += '<div style="font-size:9px;font-weight:700;color:var(--text-tertiary);text-transform:uppercase;letter-spacing:.12em;margin-bottom:10px">Reviews by release year</div>'
          html += '<div style="display:flex;gap:3px;align-items:flex-end;height:60px;overflow:hidden">'
          ps.reviewsByYearData.forEach(function(d) {
            var h = Math.max(4, (d.count / maxYear) * 52)
            html += '<div style="flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;min-width:0" title="' + d.year + ': ' + d.count + ' reviews">'
            html += '<div style="width:100%;border-radius:4px 4px 0 0;height:' + h + 'px;background:#6050c8;opacity:.7;min-height:4px"></div>'
            html += '<div style="font-size:8px;color:var(--text-tertiary);font-weight:500">' + (d.year % 100) + '</div>'
            html += '</div>'
          })
          html += '</div></div>'
        }
        html += '</div>'
      }

      // most reviewed games
      if (ps.topReviewedGames && ps.topReviewedGames.length > 0) {
        html += '<div style="background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:12px 14px;margin-bottom:10px">'
        html += '<div style="font-size:9px;font-weight:700;color:var(--text-tertiary);text-transform:uppercase;letter-spacing:.12em;margin-bottom:10px">Most reviewed games</div>'
        ps.topReviewedGames.forEach(function(g, i) {
          html += '<a href="/games/' + g.slug + '" style="display:flex;align-items:center;gap:10px;padding:10px 0;' + (i > 0 ? 'border-top:1px solid var(--border-subtle);' : '') + 'text-decoration:none;transition:opacity .15s" onmouseover="this.style.opacity=\'.75\'" onmouseout="this.style.opacity=\'1\'">'
          if (g.cover) {
            html += '<img src="' + g.cover + '" alt="" style="width:32px;height:42px;border-radius:5px;object-fit:cover;flex-shrink:0;border:1px solid var(--border)" loading="lazy">'
          } else {
            html += '<div style="width:32px;height:42px;border-radius:5px;background:var(--tag-bg);display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0">🎮</div>'
          }
          html += '<div style="flex:1;min-width:0">'
          html += '<div style="font-size:13px;font-weight:500;color:var(--text-primary);white-space:nowrap;overflow:hidden;text-overflow:ellipsis">' + g.title + '</div>'
          html += '<div style="font-size:11px;color:var(--text-tertiary);margin-top:2px">' + g.reviews + ' ' + (g.reviews === 1 ? 'review' : 'reviews') + '</div>'
          html += '</div>'
          if (g.avg) {
            var avgColor = parseFloat(g.avg) >= 9 ? '#4ade80' : parseFloat(g.avg) >= 7 ? '#60a5fa' : parseFloat(g.avg) >= 5 ? '#fbbf24' : '#f87171'
            html += '<div style="width:34px;height:34px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:700;font-family:\'Century Schoolbook\',Georgia,serif;color:' + avgColor + ';background:' + avgColor + '1a;border:1px solid ' + avgColor + '33;flex-shrink:0">' + g.avg + '</div>'
          }
          html += '</a>'
        })
        html += '</div>'
      }

      el.innerHTML = html
    }

    if (activeTab === 'stats') buildPlatformStats()

    function toggleTheme() {
      var h=document.documentElement,n=h.getAttribute('data-theme')==='light'?'dark':'light';
      h.setAttribute('data-theme',n);localStorage.setItem('theme',n);updateIcon(n);
      h.style.setProperty('--game-accent', n === 'light' ? accentLight : accentDark);
      h.style.setProperty('--accent-rgb', accentRgb);
      if (activeTab === 'stats') {
        var el = document.getElementById('platformStatsContent')
        if (el) { el.innerHTML = ''; buildPlatformStats() }
      }
    }
    function updateIcon(t) {
      var i=document.getElementById('themeIcon');
      if (!i) return;
      i.innerHTML=t==='dark'
        ?'<path d="M15 10.5A7 7 0 016.5 3a7 7 0 100 12 7 7 0 008.5-4.5z"/>'
        :'<circle cx="9" cy="9" r="3.5"/><path d="M9 1v2M9 15v2M1 9h2M15 9h2M3.22 3.22l1.42 1.42M13.36 13.36l1.42 1.42M13.36 4.64l1.42-1.42M3.22 14.78l1.42-1.42"/>';
    }
    window.toggleTheme = function(t) {
      toggleTheme(t);
      var m = document.getElementById('meta-theme-color');
      if (m) m.content = t === 'dark' ? '#0f0f0e' : '#f9f8f6';
    };

    var saved=document.documentElement.getAttribute('data-theme')||'light';
    updateIcon(saved);

  </script>

<nav class="mobile-nav" aria-label="Mobile navigation">
  <a href="/" class="mn-item">
    <span class="mn-icon"><svg width="20" height="20" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 7L9 2L16 7V16H11V11H7V16H2V7Z"/></svg></span>
    <span class="mn-label">Home</span>
  </a>
  <a href="/search" class="mn-item">
    <span class="mn-icon"><svg width="20" height="20" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="8" r="5"/><path d="M13 13l3 3"/></svg></span>
    <span class="mn-label">Search</span>
  </a>
  <a href="/hot-takes" class="mn-item">
    <span class="mn-icon"><svg width="20" height="20" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 2c0 3-2 4-2 7h4c0-3-2-4-2-7z"/><path d="M7 9c-1.5 1-2 2.5-2 4a4 4 0 008 0c0-1.5-.5-3-2-4"/><path d="M9 16v-3"/></svg></span>
    <span class="mn-label">Hot Takes</span>
  </a>
  {isLoggedIn ? (
    <>
      <a href="/notifications" class="mn-item">
        <span class="mn-icon"><svg width="20" height="20" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M13.5 6a4.5 4.5 0 0 0-9 0c0 5-2 6.5-2 6.5h13s-2-1.5-2-6.5"/><path d="M10.3 15a1.5 1.5 0 0 1-2.6 0"/></svg></span>
        <span class="mn-label">Notifs</span>
      </a>
      <a href="/profile" class="mn-item">
        <span class="mn-icon"><svg width="20" height="20" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="7" r="3.5"/><path d="M3 16c0-3.31 2.69-6 6-6s6 2.69 6 6"/></svg></span>
        <span class="mn-label">Profile</span>
      </a>
    </>
  ) : (
    <a href="/signin" class="mn-item">
      <span class="mn-icon"><svg width="20" height="20" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="7" r="3.5"/><path d="M3 16c0-3.31 2.69-6 6-6s6 2.69 6 6"/></svg></span>
      <span class="mn-label">Sign In</span>
    </a>
  )}
</nav>
</body>
</html>
