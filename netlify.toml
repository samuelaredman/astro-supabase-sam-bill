[build]
  publish = "dist"
  command = "npm run build"

# Hosts the Netlify Image CDN is allowed to fetch and optimize. Must stay in
# sync with CDN_HOSTS in src/utils/imageCdn.ts. Full-match regex.
[images]
  remote_images = [
    "https://images\\.igdb\\.com/.*",
    "https://bzlwwtatoiyzwirerify\\.supabase\\.co/.*"
  ]

[template]
  required-extensions = ["supabase"]
